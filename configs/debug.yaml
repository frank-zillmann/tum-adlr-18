# Lightweight config for laptop debugging (4 cores, 16GB, no GPU)
# Only specify values you want to override; defaults in train_config.py apply
n_envs: 1
n_steps: 32
batch_size: 32
total_timesteps: 64
eval_freq: 32
n_eval_episodes: 1
log_dir: data/debug-logs
benchmark: true